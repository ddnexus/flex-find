_missing_fields:
  missing:
    field: <<missing>>

with_scope:
- query:
    match_all: {}
  filter:
    and:
      - term: <<terms= ~ >>
      - <<_missing_fields= ~ >>
      - <<filters= ~ >>
  sort: <<sort= ~ >>
