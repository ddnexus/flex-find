_missing_fields:
  missing:
    field: <<missing>>

scope:
- query:
    query_string:
      query: <<query= '*' >>
  filter:
    and:
      - term: <<terms= ~ >>
      - <<_missing_fields= ~ >>
      - <<filters= ~ >>
  sort: <<sort= ~ >>

ids:
- query:
    terms:
      _id: <<ids>>
